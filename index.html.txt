<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ВСети - Моя Страница</title>
    <style>
        :root { --vk-blue: #0077ff; --vk-bg: #edeef0; --vk-text: #2a5885; }
        body { font-family: -apple-system, system-ui, Helvetica, sans-serif; background: var(--vk-bg); margin: 0; }
        header { background: #fff; padding: 10px; border-bottom: 1px solid #ddd; text-align: center; color: var(--vk-blue); font-weight: bold; position: sticky; top: 0; z-index: 100; }
        
        .container { display: flex; justify-content: center; padding: 20px; gap: 20px; max-width: 1000px; margin: 0 auto; }
        
        /* Боковое меню */
        nav { width: 160px; position: sticky; top: 70px; height: fit-content; }
        nav div { padding: 10px; cursor: pointer; color: var(--vk-text); border-radius: 6px; font-size: 14px; }
        nav div:hover { background: #e1e3e6; }
        nav div.active { font-weight: bold; }

        main { width: 550px; }
        .card { background: #fff; padding: 20px; border-radius: 12px; border: 1px solid #e1e3e6; margin-bottom: 15px; }

        /* Профиль */
        .profile-header { display: flex; gap: 20px; }
        .profile-avatar { width: 120px; height: 120px; background: #eee; border-radius: 8px; flex-shrink: 0; border: 1px solid #ddd; }
        .profile-info h2 { margin: 0 0 10px 0; color: #000; }
        .info-row { font-size: 13px; margin-bottom: 5px; color: #555; }
        .info-row span { color: var(--vk-text); }

        /* Посты */
        .post-header { display: flex; align-items: center; gap: 10px; font-weight: bold; color: var(--vk-text); margin-bottom: 10px; }
        .mini-avatar { width: 35px; height: 35px; background: #4a76a8; border-radius: 50%; }
        .post-content { font-size: 14px; margin-bottom: 10px; }
        .post-footer { color: #818c99; font-size: 13px; cursor: pointer; }

        #profile-page, #messenger-page { display: none; }
    </style>
</head>
<body>

<header>ВСети</header>

<div class="container">
    <nav>
        <div id="m_profile" onclick="showPage('profile')">Моя страница</div>
        <div id="m_news" class="active" onclick="showPage('news')">Новости</div>
        <div id="m_msg" onclick="showPage('messenger')">Мессенджер</div>
    </nav>

    <!-- ЛЕНТА НОВОСТЕЙ -->
    <main id="news-page">
        <div class="card">
            <textarea id="postText" placeholder="Что нового?" style="width:100%; border:1px solid #ddd; padding:10px; border-radius:8px;"></textarea>
            <button onclick="addPost()" style="margin-top:10px; background:var(--vk-blue); color:#fff; border:none; padding:7px 15px; border-radius:5px; cursor:pointer; float:right;">Опубликовать</button>
            <div style="clear:both;"></div>
        </div>
        <div id="feed"></div>
    </main>

    <!-- СТРАНИЦА ПРОФИЛЯ -->
    <main id="profile-page">
        <div class="card profile-header">
            <div class="profile-avatar"><img src="https://placeholder.com" style="width:100%; border-radius:8px;"></div>
            <div class="profile-info">
                <h2>Разработчик Сайта</h2>
                <div class="info-row">День рождения: <span>30 апреля</span></div>
                <div class="info-row">Город: <span>GitHub City</span></div>
                <div class="info-row">Статус: <span>Создаю свою соцсеть! 😎</span></div>
            </div>
        </div>
        <div class="card">
            <strong>О себе:</strong><br>
            <p style="font-size:13px; color:#333;">Я учусь кодить и это мой первый проект на GitHub.</p>
        </div>
    </main>

    <!-- МЕССЕНДЖЕР -->
    <main id="messenger-page">
        <div class="card">
            <h3>Мессенджер</h3>
            <p style="color:#888;">Здесь будут твои диалоги...</p>
        </div>
    </main>
</div>

<script>
    function showPage(page) {
        // Скрываем все страницы
        document.getElementById('news-page').style.display = 'none';
        document.getElementById('profile-page').style.display = 'none';
        document.getElementById('messenger-page').style.display = 'none';
        
        // Показываем нужную
        document.getElementById(page + '-page').style.display = 'block';
        
        // Меняем активный пункт меню
        document.querySelectorAll('nav div').forEach(el => el.classList.remove('active'));
        if(page === 'profile') document.getElementById('m_profile').classList.add('active');
        if(page === 'news') document.getElementById('m_news').classList.add('active');
        if(page === 'messenger') document.getElementById('m_msg').classList.add('active');
    }

    function addPost() {
        const text = document.getElementById('postText').value;
        if (!text) return;
        const feed = document.getElementById('feed');
        const post = document.createElement('div');
        post.className = 'card';
        post.innerHTML = `
            <div class="post-header"><div class="mini-avatar"></div>Разработчик</div>
            <div class="post-content">${text}</div>
            <div class="post-footer">❤ Лайк</div>
        `;
        feed.prepend(post);
        document.getElementById('postText').value = '';
    }
</script>

</body>
</html>

